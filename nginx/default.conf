# HTTP — redirect all traffic to HTTPS and serve ACME challenge for Certbot
server {
    listen 80;
    server_name 0nt5c.irhspur.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS — proxy to app
server {
    listen 443 ssl;
    server_name 0nt5c.irhspur.com;

    ssl_certificate     /etc/letsencrypt/live/0nt5c.irhspur.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/0nt5c.irhspur.com/privkey.pem;

    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    client_max_body_size 10M;

    location / {
        proxy_pass         http://app:5000;
        proxy_http_version 1.1;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }
}
